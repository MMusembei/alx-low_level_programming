#include <stdio.h>
/**
 * main - prints all base single digits starting from 0
 * Return: 0 when executed
 */

int main(void)
{
	int i;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
