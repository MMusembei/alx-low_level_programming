#include <stdio.h>

/**
 * main - prints the alphabet in lowercase
 * Return: 0 when executed
 */

int main(void)
{
	char az;

	for (az = 'a'; az <= 'z'; az++)
	{
		putchar(az);

	}
	putchar('\n');
	return (0);
}
