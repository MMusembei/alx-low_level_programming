#include "main.h"

/**
 * _abs - Calc the absolute value of an int
 *  @n: the integer to check
 *  Return: The absolute int value
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
