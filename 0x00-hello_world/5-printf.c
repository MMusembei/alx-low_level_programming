#include <stdio.h>

/**
 * main - using the printf fxn, a phrase given is printed
 * Return: 0 if excuted perfectly, non-zero otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);

}
