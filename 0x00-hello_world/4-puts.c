#include <stdio.h>

/**
 * Use the puts function to print out an exact statement given
 * After the output, print a newline
 * Program should end with value 0
 */
int main(void)
{

	puts("\"Programming is like building a multilingual puzzle");
	return(0);
}
