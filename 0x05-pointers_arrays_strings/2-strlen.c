#include "main.h"

/**
 * _strlen - returns the str length
 * @s: str to evaluate
 * Return: the str length
 */
int _strlen(char *s)
{
	int i;

	i = 0;

	while (s[i] != '\0')
	{
		i++
	}

	return (i);
}

